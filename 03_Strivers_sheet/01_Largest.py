nums = list(map(int,input("Enter nums: ").split()))

n = len(nums)
def largest(nums):
    maximum = 0
    for i in range(0,n):
        if max > nums[i]:
            i+=1
        else:
            maximum = nums [i]
    return maximum

print(maximum)

